# Zeabur 部署配置 — Zeabur 会自动识别，但显式声明更稳
# 这个文件放在 server/ 目录根

[build]
command = "npm install && npm run build"
# 启动前跑一次 migration（生产环境用 deploy，幂等）
# 也可以在 Zeabur 的启动命令里手动写

[[ports]]
port = 3000
type = "HTTP"
