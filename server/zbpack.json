# Zeabur 部署配置 (TOML 格式)
# https://zeabur.com/docs/marketplace/format

[build]
command = "npm install && npm run build"

[[ports]]
port = 3000
type = "HTTP"

# 健康检查路径（Zeabur 用来判断服务是否启动）
# Zeabur 会通过 /health 端点判断服务存活
